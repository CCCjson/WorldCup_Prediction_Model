name: wc2026
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pandas>=2.2
  - numpy>=1.26
  - pyarrow>=15
  - scipy>=1.11
  - scikit-learn>=1.4
  - statsmodels>=0.14
  - lightgbm>=4.1
  - matplotlib>=3.8
  - pytest>=8.0
  # Bayesian model (MS3) — heavier; conda-forge build preferred
  - pymc>=5.10
  - pip
  - pip:
      - kaggle>=1.6
      - requests>=2.31
      - beautifulsoup4>=4.12
      - streamlit>=1.33
      - playwright>=1.40   # 之后需运行 `playwright install chromium`
